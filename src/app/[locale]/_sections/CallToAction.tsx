import * as React from "react";

interface ICallToActionProps {}

const CallToAction: React.FunctionComponent<ICallToActionProps> = (props) => {
  return <button>Hell from cta</button>;
};

export default CallToAction;
